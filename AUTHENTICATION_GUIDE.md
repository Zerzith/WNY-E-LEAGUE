# คู่มือระบบสมัครสมาชิกและเข้าสู่ระบบ
## วิทยาลัยเทคนิควังน้ำเย็น Esports Management System

---

## 📋 สารบัญ
1. [ภาพรวมระบบ](#ภาพรวมระบบ)
2. [เทคโนโลยีที่ใช้](#เทคโนโลยีที่ใช้)
3. [โครงสร้างไฟล์](#โครงสร้างไฟล์)
4. [การทำงานของระบบ](#การทำงานของระบบ)
5. [คู่มือการใช้งาน](#คู่มือการใช้งาน)
6. [การตั้งค่าและติดตั้ง](#การตั้งค่าและติดตั้ง)

---

## ภาพรวมระบบ

ระบบนี้เป็นระบบจัดการการแข่งขัน Esports สำหรับวิทยาลัยเทคนิควังน้ำเย็น มีระบบ **Authentication** ที่สมบูรณ์ ประกอบด้วย:

### ✅ ฟีเจอร์ที่มีอยู่แล้ว
- **สมัครสมาชิก (Register)** - รองรับการสร้างบัญชีใหม่
- **เข้าสู่ระบบ (Login)** - รองรับ Email + Password
- **ออกจากระบบ (Logout)** - ล้าง session และ redirect
- **จัดการสถานะผู้ใช้** - ใช้ React Context API
- **ควบคุมสิทธิ์การเข้าถึง** - Role-based (user/admin)
- **แสดงข้อความ Error ภาษาไทย** - แปลจาก Firebase Error Codes
- **บันทึกข้อมูลผู้ใช้** - Firestore Database

---

## เทคโนโลยีที่ใช้

### Frontend Stack
| เทคโนโลยี | เวอร์ชัน | หน้าที่ |
|-----------|----------|---------|
| **React** | 18.3.1 | UI Framework |
| **TypeScript** | 5.6.3 | Type Safety |
| **Vite** | 7.3.1 | Build Tool |
| **Tailwind CSS** | 3.4.19 | Styling |
| **Wouter** | 3.9.0 | Routing |
| **Framer Motion** | 11.18.2 | Animations |

### Authentication & Database
| เทคโนโลยี | หน้าที่ |
|-----------|---------|
| **Firebase Authentication** | จัดการ Login/Register |
| **Firebase Firestore** | เก็บข้อมูลผู้ใช้ |
| **React Hook Form** | จัดการฟอร์ม |
| **Zod** | Validation |

### UI Components
- **Radix UI** - Accessible components
- **Lucide React** - Icons
- **Shadcn/ui** - Pre-built components

---

## โครงสร้างไฟล์

```
esports-system/
├── client/src/
│   ├── pages/
│   │   └── Login.tsx              # หน้า Login/Register
│   ├── hooks/
│   │   └── use-auth.tsx           # Authentication Hook
│   ├── lib/
│   │   └── firebase.ts            # Firebase Configuration
│   ├── components/
│   │   ├── Navigation.tsx         # เมนูนำทาง (แสดงสถานะ Login)
│   │   └── Footer.tsx
│   └── App.tsx                    # Main App Component
├── shared/
│   └── schema.ts                  # Database Schema
└── .env.example                   # ตัวอย่างตัวแปรสภาพแวดล้อม
```

---

## การทำงานของระบบ

### 1. 📝 การสมัครสมาชิก (Register Flow)

```
ผู้ใช้กรอกฟอร์ม
    ↓
ตรวจสอบ Validation
    ↓
Firebase: createUserWithEmailAndPassword()
    ↓
อัปเดต Profile (displayName)
    ↓
บันทึกข้อมูลเพิ่มเติมใน Firestore
    ↓
แสดง Toast "สมัครสมาชิกสำเร็จ"
    ↓
Redirect ไปหน้าแรก (/)
```

**ข้อมูลที่เก็บใน Firestore:**
```typescript
{
  displayName: "นายสมชาย ใจดี",
  studentId: "6XXXXXXXXX",
  role: "user",
  email: "example@email.com",
  createdAt: "2026-01-27T14:30:00.000Z"
}
```

### 2. 🔐 การเข้าสู่ระบบ (Login Flow)

```
ผู้ใช้กรอก Email + Password
    ↓
Firebase: signInWithEmailAndPassword()
    ↓
onAuthStateChanged() ตรวจจับการเปลี่ยนแปลง
    ↓
ดึงข้อมูล role จาก Firestore
    ↓
อัปเดต AuthContext
    ↓
แสดง Toast "เข้าสู่ระบบสำเร็จ"
    ↓
Redirect ไปหน้าแรก (/)
```

### 3. 🚪 การออกจากระบบ (Logout Flow)

```
ผู้ใช้คลิกปุ่ม Logout
    ↓
เรียก signOut() จาก useAuth
    ↓
Firebase: firebaseSignOut(auth)
    ↓
ล้าง user state
    ↓
Redirect ไปหน้า /login
```

### 4. 🛡️ Protected Routes

ระบบตรวจสอบสิทธิ์การเข้าถึงหน้าต่างๆ:

| หน้า | เงื่อนไข | การทำงาน |
|------|----------|----------|
| `/login` | ถ้า user มีอยู่แล้ว | Redirect ไป `/` |
| `/register-team` | ต้อง Login | ตรวจสอบ `user !== null` |
| `/admin` | ต้องเป็น Admin | ตรวจสอบ `user.role === "admin"` |
| `/chat` | ต้อง Login | ตรวจสอบ `user !== null` |

---

## คู่มือการใช้งาน

### สำหรับผู้ใช้ทั่วไป

#### 1. สมัครสมาชิก
1. เปิดเว็บไซต์และคลิก **"เข้าสู่ระบบ"** ที่มุมขวาบน
2. คลิก **"ยังไม่มีบัญชี? สมัครสมาชิก"**
3. กรอกข้อมูล:
   - **ชื่อ-นามสกุล**: นายสมชาย ใจดี
   - **รหัสบัตรนักศึกษา**: 6XXXXXXXXX
   - **อีเมล**: example@email.com
   - **รหัสผ่าน**: อย่างน้อย 6 ตัวอักษร
4. คลิก **"สมัครสมาชิก"**
5. รอระบบประมวลผล (จะมี Loading Spinner)
6. เมื่อสำเร็จจะแสดง Toast และเข้าสู่ระบบอัตโนมัติ

#### 2. เข้าสู่ระบบ
1. คลิก **"เข้าสู่ระบบ"** ที่มุมขวาบน
2. กรอก **อีเมล** และ **รหัสผ่าน**
3. คลิก **"เข้าสู่ระบบ"**
4. เมื่อสำเร็จจะแสดงชื่อผู้ใช้ที่มุมขวาบน

#### 3. ออกจากระบบ
1. คลิกไอคอน **Logout** (ประตูทางออก) ที่มุมขวาบน
2. ระบบจะออกจากระบบและ redirect ไปหน้า Login

### สำหรับ Admin

Admin จะมีเมนูเพิ่มเติม:
- **จัดการระบบ** - เข้าถึง Admin Dashboard
- สามารถจัดการทีม, การแข่งขัน, คะแนน

---

## การตั้งค่าและติดตั้ง

### 1. ติดตั้ง Dependencies

```bash
cd esports-system
pnpm install
```

### 2. ตั้งค่า Firebase

สร้างไฟล์ `.env` จาก `.env.example`:

```bash
cp .env.example .env
```

แก้ไขไฟล์ `.env`:

```env
VITE_FIREBASE_API_KEY=your_api_key_here
VITE_FIREBASE_AUTH_DOMAIN=your_project.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your_project_id
VITE_FIREBASE_STORAGE_BUCKET=your_project.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
VITE_FIREBASE_APP_ID=your_app_id
```

### 3. ตั้งค่า Firestore Database

ใน Firebase Console:
1. ไปที่ **Firestore Database**
2. สร้าง Collection ชื่อ **"users"**
3. ตั้งค่า Security Rules:

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Users collection
    match /users/{userId} {
      allow read: if request.auth != null;
      allow write: if request.auth != null && request.auth.uid == userId;
    }
    
    // Teams collection
    match /teams/{teamId} {
      allow read: if true;
      allow create: if request.auth != null;
      allow update, delete: if request.auth != null && 
        (get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'admin');
    }
    
    // Events, Matches, Chat
    match /{document=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
```

### 4. เปิดใช้งาน Authentication

ใน Firebase Console:
1. ไปที่ **Authentication**
2. เปิดใช้งาน **Email/Password**
3. (Optional) เปิดใช้งาน **Google Sign-In**

### 5. รันโปรเจกต์

```bash
pnpm dev
```

เปิดเบราว์เซอร์ที่ `http://localhost:5173`

---

## 🔧 การแก้ไขและปรับแต่ง

### เพิ่ม Error Message ภาษาไทย

แก้ไขไฟล์ `client/src/lib/firebase.ts`:

```typescript
export function mapFirebaseErrorToThaiMessage(errorCode: string): string {
  switch (errorCode) {
    case 'auth/user-not-found':
      return 'ไม่พบผู้ใช้นี้ในระบบ';
    case 'auth/wrong-password':
      return 'รหัสผ่านไม่ถูกต้อง';
    // เพิ่ม error codes อื่นๆ ตามต้องการ
    default:
      return 'เกิดข้อผิดพลาด กรุณาลองใหม่อีกครั้ง';
  }
}
```

### เปลี่ยนสี Theme

แก้ไขไฟล์ `tailwind.config.ts`:

```typescript
theme: {
  extend: {
    colors: {
      primary: '#004080', // สีน้ำเงิน
      accent: '#FF6B35',  // สีส้ม
    }
  }
}
```

### เพิ่มฟิลด์ในฟอร์มสมัครสมาชิก

แก้ไขไฟล์ `client/src/pages/Login.tsx`:

```typescript
const [phoneNumber, setPhoneNumber] = useState("");

// เพิ่มใน form
<div className="space-y-2">
  <Label>เบอร์โทรศัพท์</Label>
  <Input 
    value={phoneNumber} 
    onChange={(e) => setPhoneNumber(e.target.value)} 
    placeholder="0812345678" 
  />
</div>

// เพิ่มใน Firestore
await setDoc(doc(db, "users", userCredential.user.uid), {
  displayName,
  studentId,
  phoneNumber, // เพิ่มบรรทัดนี้
  role: "user",
  email: userCredential.user.email,
  createdAt: new Date().toISOString()
});
```

---

## 📊 สรุป

### ✅ ระบบที่มีอยู่แล้ว
- สมัครสมาชิก (Register)
- เข้าสู่ระบบ (Login)
- ออกจากระบบ (Logout)
- จัดการสถานะผู้ใช้ (AuthContext)
- ควบคุมสิทธิ์การเข้าถึง (Protected Routes)
- แสดง Error ภาษาไทย
- บันทึกข้อมูลใน Firestore

### 🎯 พร้อมใช้งาน
ระบบสมัครสมาชิกและเข้าสู่ระบบ**พร้อมใช้งานแล้ว 100%** เพียงแค่ตั้งค่า Firebase Configuration ในไฟล์ `.env` ก็สามารถใช้งานได้ทันที

### 📝 หมายเหตุ
- ระบบใช้ Firebase Authentication เป็นหลัก
- ข้อมูลผู้ใช้เก็บใน Firestore Database
- รองรับการขยายเพิ่ม Google Sign-In ในอนาคต
- มี Error Handling ที่ครบถ้วน
- UI/UX ออกแบบเป็น Gaming/Esports Theme

---

**พัฒนาโดย:** ระบบจัดการ Esports วิทยาลัยเทคนิควังน้ำเย็น  
**เวอร์ชัน:** 1.0.0  
**วันที่อัปเดตล่าสุด:** 27 มกราคม 2026
