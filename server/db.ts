// This file is intentionally minimal because the application uses Firebase Firestore.
// We do not need a server-side PostgreSQL connection.
// If you need server-side logic that requires a database, consider using the Firestore Admin SDK.

export const db = null;
